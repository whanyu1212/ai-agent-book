# 复制为 .env 后填入你的 key
# 本实验只需要 OpenAI（chat + embeddings 均用 OpenAI）
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 可选：作为"较小/受限"模型来体现主动发现的价值，默认 gpt-4o-mini
# MODEL=gpt-4o-mini

# 可选：嵌入模型，默认 text-embedding-3-small
# EMBED_MODEL=text-embedding-3-small
