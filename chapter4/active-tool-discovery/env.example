# 复制为 .env 后填入你的 key。
# 聊天后端由仓库共享逻辑解析；OpenAI 直连时 chat 与 embeddings 都使用 OpenAI。
# 暂时没有 key 时，可先跑离线机制自检：python demo.py --offline
OPENAI_API_KEY=your-openai-api-key

# 通用回退：若没有 OPENAI_API_KEY 但设置了 OPENROUTER_API_KEY，chat 会改走
# OpenRouter。已知模型名会映射，未知名称保持不变而非静默改用其他模型。这里的
# OpenRouter 没有 embeddings 接口，因此工具检索会使用本地哈希嵌入。
# OPENROUTER_API_KEY=your-openrouter-api-key

# 可选：对话模型，默认 gpt-5.6-luna。
# MODEL=gpt-5.6-luna

# 可选：嵌入模型，仅 OpenAI 直连路径使用。
# EMBED_MODEL=text-embedding-3-small
